import * as path from "path";

// import * as case1a from "./case1a";

export function loadTests(baseDir: string): void
{
    var thisDir = path.resolve(baseDir, "case6");
        describe('case6: Intermediate preservation using .PRECIOUS', function ()
        {
            // case1a.loadTests(thisDir, 1);
        });
    }
