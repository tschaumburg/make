import * as path from "path";

// import * as case1a from "./case1a";

export function loadTests(baseDir: string): void
{
    var thisDir = path.resolve(baseDir, "case5");
        describe('case5: Intermediate preservation using .SECONDARY', function ()
        {
            // case1a.loadTests(thisDir, 1);
        });
    }
